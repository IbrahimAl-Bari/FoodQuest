-- Each key is the stable ID used by the corresponding ObbyStart and
-- ObbyFinish world parts. Rewards are granted only by ObbyRewardService.
return {
	StarterObby = {
		minimumCompletionSeconds = 10,
		rewards = {
			Flour = 3,
			Tomato = 2,
			Cheese = 1,
		},
	},
}
