-- Counter IDs are stable so restaurant layouts and upgrades can reference them.
return {
	defaultCounterId = "StarterCounter",
	counters = {
		StarterCounter = {
			displayName = "Display Counter",
			slotCount = 3,
		},
	},
}
