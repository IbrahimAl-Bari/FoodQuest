return {
	-- How often a new customer arrives (seconds)
	arrivalIntervalSeconds = 12,
	-- How long the customer waits at the counter before leaving (seconds)
	patienceSeconds = 30,
	-- Time the customer stands at the counter inspecting (seconds)
	inspectSeconds = 1,
	-- Extra wait before leaving when no food is served
	noFoodWaitSeconds = 3,
	-- Walk speed for the NPC
	walkSpeed = 16,
}
